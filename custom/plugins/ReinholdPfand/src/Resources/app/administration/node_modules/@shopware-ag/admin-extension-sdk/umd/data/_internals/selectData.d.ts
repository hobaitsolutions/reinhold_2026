import type { ShopwareMessageTypes } from '../../messages.types';
export declare function selectData(sourceData: unknown, selectors?: string[], messageType?: keyof ShopwareMessageTypes, origin?: string): unknown;
